### Result
{"scene":"gateway-instrumentation","startedAt":"2026-09-13T18:16:52.471Z","endedAt":"2026-09-13T18:17:13.160Z","marks":[{"time":0,"label":"Metrics recording is enabled; anonymous reporting is disabled"},{"time":5.614,"label":"Read native Gateway instruments and the zero-turn baseline"},{"time":11.647,"label":"Inspect recorded startup measurements"},{"time":17.18,"label":"Return to latency instruments"}],"nativeBackendProof":false}
### Ran Playwright code
```js
await (async page => {
  await page.getByRole('heading',{name:'Telemetry controls',exact:true}).waitFor();
  const startedAt=new Date().toISOString(),marks=[];
  await page.screencast.start({path:'recordings/20260913/raw/04-gateway-instrumentation-light.webm',size:{width:1600,height:1000}});
  const start=Date.now(),mark=label=>marks.push({time:(Date.now()-start)/1000,label});
  let annotation;
  try {
    mark('Metrics recording is enabled; anonymous reporting is disabled');
    await page.getByRole('switch',{name:'Record metrics',exact:true}).scrollIntoViewIfNeeded();
    await page.waitForTimeout(5500);
    await page.getByRole('navigation',{name:'Main navigation'}).getByRole('button',{name:'Developer',exact:true}).click();
    await page.locator('main nav button').filter({hasText:/^Telemetry$/}).click();
    await page.getByText('Turn latency (p50)',{exact:true}).waitFor();
    annotation=await page.screencast.showOverlay('<div style="position:fixed;left:310px;right:75px;bottom:20px;padding:14px 18px;background:#eef4ff;color:#153052;border:1px solid #a5bce0;border-radius:10px;font:17px/1.45 system-ui;box-shadow:0 5px 25px #0003"><strong>Demo note</strong> · Gateway request and process instruments contain data. Completed native turns remain zero.</div>');
    mark('Read native Gateway instruments and the zero-turn baseline');
    await page.waitForTimeout(6000);
    await page.getByRole('button',{name:/^Startup/}).click();
    mark('Inspect recorded startup measurements');
    await page.waitForTimeout(5500);
    await page.getByRole('button',{name:'Latency',exact:true}).click();
    mark('Return to latency instruments');
    await page.waitForTimeout(3500);
    await annotation.dispose();annotation=null;
    return {scene:'gateway-instrumentation',startedAt,endedAt:new Date().toISOString(),marks,nativeBackendProof:false};
  } finally {if(annotation)await annotation.dispose();await page.screencast.stop();}
}
)(page);
```
### Page
- Page URL: http://localhost:5599/developer?tab=telemetry
- Page Title: Kiro Crew
- Console: 30 errors, 0 warnings
### Events
- New console entries: .playwright-cli/console-2026-09-13T18-12-14-790Z.log#L29-L30
