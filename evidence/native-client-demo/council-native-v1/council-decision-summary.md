# Agreed council decisions for native controls

Root accepted the shared material findings from Grok and Opus. Both reviewers returned **CHANGES REQUIRED** for the original frozen ten-slide candidate. Implementation is assigned; this receipt does not mark those edits complete.

- **D1: Use the real result frame as each clip poster.** Owner `/root/native_media_cuts`. Update posters only; retain MP4 bytes, source captures and cuts.
- **D2: Make owner-editable Crew configuration and no enterprise policy floor visible.** Owner `/root/native_control_deck`. Architecture slide; preserve one endpoint-control box.
- **D3: Label private-memory refusal as a conversation assignment guard before tools.** Owner `/root/native_control_deck`. No restricted-user RBAC, filesystem denial or host-tool enforcement claim.
- **D4: Refresh authority receipt hash and include native MCP media/authentication receipts.** Owner `/root/native_control_deck`. Final notes, supporting links and allowed-file server.
- **D5: Add readable admin context and separate posture, telemetry, health logs and audit evidence.** Owner `/root/native_control_deck`. Admin slide plus separately reviewed screenshot guide.
- **D6: Keep joined-receipt attribution and unrecorded host-case limits visible.** Owner `/root/native_control_deck`. No immutable-policy, full enterprise acceptance, pending-PID overlap or complete-original-collector claim.

Keep the existing MP4s and cut boundaries. The one-second shared lead-in is disclosed source overlap, and corrected result posters address the misleading static view. Use consistent displayed duration rounding while preserving precise media metadata.

The post-council deck worker will apply No AI Slop after the revisions. Root will run browser checks on the changed presentation and a fresh read-only scope/diff review. The external model reviews bind the original candidate below; they do not constitute exact-model review of the changed final derivative.

## Review provenance

- HTML: `ac06b8132961dfc35cccc5d1c470ca0ff5b395ec7f7044659803f4ea7b0be8a7`.
- Notes: `612775125751c05138e39a13bd2d88814e6c58b6ef49f48d338cb69acf24f78c`.
- Manifest: `74280ad40e10efaf66fe465f0a0f4e472a20e5f60fc02f6eec2edd8a2f24fa4a`.
- Actual review response IDs: `grok-4.6` and `claude-opus-5`; each read all ten exact packet PNG payloads.
- Both launches explicitly set `xhigh`; wire effort is not independently visible. Usage accounting reports `grok-4.6-build` and, for Claude, both `claude-opus-5` and `claude-haiku-4-5-20251001`.
- Independent read-only receipt audit passed. Streams contain one successful terminal result each, valid JSON throughout, empty stderr, and only packet-local Read/read_file tool calls. No additional inference was used for the audit.

The models viewed slide pixels, notes and the concise source-bound evidence summary. They did not inspect every video frame, test playback/seek, access live AWS/backend state, read all underlying enforcement receipts or inspect the separate full admin guide. Original CUA JPEG bytes with .png names were preserved; lossless PNG encodings retain identical decoded RGB24 pixels. Names and hashes are recorded in the capture binding.
