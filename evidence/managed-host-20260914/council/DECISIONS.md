# Managed host council decisions

Grok 4.6 and Opus 5 read all 19 frozen candidate images and returned CHANGES REQUIRED. Both used the requested xhigh launch setting. The dispatcher verified model identity, all image payloads and unchanged packet bytes.

- **MH1 (accepted)** Replace stale pending text in slide 18 notes; explicitly date the retained 02:20 UTC pending summary and state the later outcomes.
- **MH2 (accepted)** Reduce control-map row padding and capture-label size; final last-row-to-footer gap is more than 89 pixels.
- **MH3 (accepted)** Shorten ui4 chapter text to the exact tool and trace, remove the short detail-height cap; full chapter panel fits without scrolling.
- **MH4 (accepted)** Use existing unretouched result frames at 24, 26 and 53 seconds for the three host posters. Original processed video bytes and manifests stay unchanged.
- **MH5 (accepted)** Keep CLI ENOENT as the immediate result, label later CLI-view readback, and put native accepted:false in slide 14 footer.
- **MH6 (accepted)** State root ownership of hook source and its precedence before the managed filesystem rule. Exact source hashes match all take snapshots.
- **MH7 (accepted_with_source_clarification)** Narrow the network title to the Crew UID firewall rule. Retain port 80 because the exact reviewed helper fixes that destination; add the source hash to notes.
- **MH8 (accepted)** Distinguish the host recording review from the managed MCP recording review.
- **MH9 (no_change_required)** Capture receipt intentionally names source slide-01.png; packet normalizes to slide-1.png. Dispatcher verifies exact PNG hashes for both reviewers. README already documents this contract.
- **MH10 (accepted)** Remove repeated host explanations from map and recording notes; preserve one precise attribution summary and the chapters/provenance.

The final deck is an author-verified derivative. The original council verdicts were not changed or represented as approval of final bytes.
