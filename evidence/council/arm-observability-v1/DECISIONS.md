# ARM and observability deck decisions

The council reviewed the frozen 12-slide candidate-v2, SHA256 `99d10eee8723b3998c40f52e902602661138e7388f0d6d3db8e2d5ef13f63dd9`. Grok 4.6 through Grok Build and Opus 5 through Claude Code both returned **CHANGES REQUIRED**. Both launches requested xhigh; the response model identities and all 12 image deliveries are bound in the council receipt. Their reviews cover the supplied slide images, text, notes and evidence summary. They did not independently verify AWS or the underlying runtime receipts.

Root read both complete reviews and accepted these revisions for the final edition:

| Slides | Decision | Evidence and treatment |
| --- | --- | --- |
| 2 | Make the reference-map limit prominent. | Preserve the accepted endpoint image byte for byte. State that native sandbox, egress-control and SEL/HMAC execution are unverified. The reported intersection-glyph appearance is retained rather than silently changing the accepted visual. |
| 3 | Mark the Gateway/Kiro CLI to MCP path as pending. | Use a dashed native path and a short pending label. Name the verified operator path, `mcp-demo → MCP → S3`. Preserve the single endpoint enclosure and component topology. Regenerate the new ARM Archify files and slide image; retain the reviewed candidate and prior editions. |
| 4 | Bind metadata denial to the actual ARM probe and state the configuration ownership limit. | `crew-imds-connect-receipt.json` records a crew-UID IPv4 TCP connection denied immediately with errno 113. No HTTP request or credential was read. This supplements the council packet, which contained configuration evidence only. Crew can edit its demo configuration; immutable policy is not established. |
| 5 | Label `crew_denied` as configured, with native execution pending. | Its expected result must not look equivalent to the three directly probed tool outcomes. |
| 6 | Use the final documented-command rehearsal and keep runs distinct. | `final-direct-mcp-rehearsal.json` passed at 15:01 UTC, after telemetry activation. Update the visible time and AWS request identifiers from that receipt. It is still a separate direct service probe with `native_crew_backend_verified=false`; it is not one end-to-end agent session. Retain the earlier 14:07 receipt. |
| 7 | State the server distribution and version limit explicitly. | The Mac runs the September 13 Nightly. EC2 runs a custom ARM repack of the frozen September 12 package, not an official Linux release. No behavioral equivalence or authenticated-session performance claim is added. |
| 8 | Separate the display window, retention and flush interval. | Show 14-day display range versus seven-day retention; preserve the 64 MiB target and ten-second local flush. The one boot sample does not establish a boot-time distribution, regardless of percentile labels in the native UI. Keep original screenshot bytes. |
| 9–10 | Label screenshot clocks and measurement definitions. | The browser displayed EDT (UTC−4); the receipts use UTC. Client CPU describes the KiroCrew process family with scheduler averages; server CPU describes the host with a one-second sample. |
| 10 | Attribute server continuity to its receipt. | Label the times as stopped and recovery **samples**, not exact UI action timestamps. Cite Gateway PID 4991, start 14:26:10 UTC, unchanged, with server checks remaining Yes. The generic collection-event table alone does not contain that process identity. |
| 12 | Make CPU credits and retained storage visible. | Standard CPU credits avoid surplus-credit charges and can throttle sustained work to baseline. Name the stopped t3a.small rollback host and its billed 24 GiB gp3 volume. Preserve the verified rate arithmetic. |

The low-priority suggestion to add port 9102 to the execution topology can be satisfied in a concise caption or speaker note if the diagram would become crowded. The dedicated observability diagram and walkthrough show the App Kit route in detail. No extra endpoint-control box should be added.

After these decisions are incorporated, run the requested No AI Slop edit, render all 12 final slides, inspect them, and bind package, layout, font, editable-table, image-byte and reimport checks to the final PPTX. The council verdict remains bound to candidate-v2. Final author validation is separate; these reviews do not approve the unseen final bytes.
