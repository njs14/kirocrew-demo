# Council decisions for the EC2 deck

Candidate SHA256: `fe7708a0689906820be49bd06baa325c93fec3465856bdc0071c2daae307cc3e`.

Grok 4.6 xhigh through Grok Build and Opus 5 xhigh through Claude Code each read all nine exact slide PNG payloads plus the supplied text/notes and sanitized evidence summary. Both returned **changes required**. `council-verified-receipt.json` binds their response model IDs, requested effort, image bytes and transcripts. The reviewers did not independently query AWS or verify source-file hashes. An initial Claude authentication failure is retained separately and does not count as a review.

The root author accepts these decisions before the requested no-ai-slop pass:

| Issue | Decision |
| --- | --- |
| Slide 3 unreadable labels | Replace the dense slide topology with a concise Archify view retaining the host, client, MCP and target boundaries. Verify its actual slide-size pixels. Retain the detailed browser view for exploration. |
| Slide 3 L0–L5/SEL overclaim | Narrow the actual deployment node to the installed demo configuration. Keep the accepted L0–L5 map in the explicitly labeled reference slide and viewer tab. |
| Slides 1, 2 and 7 scope | Put the observed direct MCP/AWS scope and pending native authentication on the slides. Preserve the accepted endpoint image bytes and add its reference caption outside the image. |
| Slide 8 usable walkthrough | Center the current live sequence on the verified client, remote workspace and three-call direct MCP probe. Present the four native turns as the next acceptance step after authentication. Crew denial must be automatic; operator rejection cannot count as that proof. |
| Slide 4 metadata boundary | State that the firewall blocks the crew UID, while mcp-demo can obtain the instance role. Include the role's SSM core permissions as well as the fixed S3 prefixes. |
| Slide 3 host ownership | Name the outer boundary as the EC2 host so the separate MCP process is clearly on the same instance. Keep region and instance type in the title/caption. |
| Slide 6 receipt identity | Add the direct-probe identity/time and unauthenticated 401. Keep the original reviewed receipt and add the fresh documented-command rehearsal as separate evidence. |
| Slide 7 version placement | Place Crew dependency-lock wording with the Crew package, separate from the official Kiro CLI artifact. Add the newly verified 24 deployed module hashes to notes. |

The root author checked the underlying diagram strings. `Policy ∩ Profile`, `Files + session state`, `Ubuntu 24.04`, port `8001` and `GetObject denied` are correct in the source. The council's alternate readings reflect insufficient pixel legibility, not confirmed source corruption. The repair addresses readability rather than changing the correct facts.

The reference map contains installed software capabilities and proposed enterprise controls. Its caption should say which checks this demo verifies, rather than assert that every untested pictured control is absent. Source presence, configured controls and observed enforcement remain separate claims.

These are author decisions based on the two reviews. The council reviewed candidate-v3; it has not approved the subsequent final bytes. After applying the decisions, the author performs the requested no-ai-slop edit and fresh package/render/visual validation, with that scope recorded separately.
