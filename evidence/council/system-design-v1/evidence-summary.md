# Recorded evidence for the system-design presentation

This is a presentation review packet. It contains the exact self-contained HTML, exact slide text and presenter notes in `notes.md`, and fourteen final-build browser viewport captures (`slide-01.jpg` through `slide-14.jpg`). The screenshots are 1280×720 captures from the delivered browser presentation. The packet manifest binds all supplied bytes. The HTML includes original evidence images and the earlier PPTX download, but opening or parsing that large file is unnecessary for this visual review. Read every JPEG as pixels and use notes for exact text.

All runtime evidence below is recorded September 13, 2026. This presentation does not poll the live system. No fresh AWS, UI, or authentication checks were performed for this council review. The native Kiro CLI sign-in and a verified native backend session remain pending in the latest source receipts (15:19 UTC). The dashboard's KAS GitHub card reads a separate identity store and does not establish standalone CLI authentication.

## Deployment and authority

CloudFormation in us-east-1 was UPDATE_COMPLETE at 14:22 UTC. The active host was t4g.xlarge ARM64, 4 vCPU, 16 GiB RAM, Ubuntu 24.04, Standard CPU credits, and 40 GiB encrypted gp3. A fresh ingress check at 14:57 UTC confirmed only TCP 22 from the operator's current IPv4 /32. Gateway 5476, MCP 8001, and Demo Observability 9102 are loopback services. SSH forwards Mac port 5599 to remote Gateway 5476. The original t3a.small is stopped; its 24 GiB disk remains for rollback and billing.

The Mac connected to the remote workspace with its local Gateway off and no local 5476 listener. Gateway, selected backend installation and workspace share the remote execution boundary. The MCP service runs separately as `mcp-demo` and uses the instance role. The role permits two demo S3 prefixes, explicitly denies reads under the denied prefix, and includes SSM core permissions. No IAM user keys or existing coding-backend sessions were copied to EC2.

The `crew` user runs Gateway, backend and workspace. The crew-UID IPv4 metadata TCP check at 14:56:15 UTC was denied immediately with errno 113. It sent no HTTP request and retrieved no token or credentials. IPv6 and native sandbox execution were not tested. Root owns core Gateway/MCP runtime files; root is trusted. Crew can edit its configuration and installed custom App Kit code. Immutable policy and tamper resistance are unverified.

## Enforcement, versions and observations

The separate direct MCP probe passed at 15:01:37 UTC and explicitly records `native_crew_backend_verified=false`. Unauthenticated access returned 401; discovery listed four tools. `read_allowed` returned 55 bytes with SHA-256 `34ebbefeb5f66527fbc80083c3b695a63e675c2093fd5f3f079a7c5bfe4b9160`, AWS request `FC4GTZS0B83EQ5W1`. `mcp_denied` returned `tool_grant_denied` before AWS dispatch. `iam_denied` returned S3 AccessDenied HTTP 403, AWS request `BA5BT7K9765938BT`. These service checks do not prove a native Crew session. `crew_denied` is a configured rule whose automatic pre-approval, pre-MCP native refusal remains pending. The three routed native calls need their own real approval flow after sign-in.

Mac: `0.7.0-nightly.20260913t061222`; 24 selected paths in two architecture bundles gave 48 passing RECORD checks. Three selected paths differ from September 12. Server: unofficial custom Linux ARM64 repack of frozen `0.7.0-nightly.20260912t060850`; 24 selected modules match that baseline, with 48 locked dependencies. Official Kiro CLI 2.21.4 ARM is installed separately. No behavioral equivalence or authenticated-session performance is established.

Native Gateway telemetry: local export every 10 seconds, seven-day retention and 64 MiB retention target. Active writers may exceed the target temporarily. No OTLP endpoint; anonymous usage reporting off. The screenshot's 14-day display window is distinct from retention. Its one boot observation cannot establish a latency distribution. Native model-turn metrics remain unverified.

Custom App Kit v1.0.1 uses the authenticated Gateway proxy and a loopback backend. Mac and EC2 collectors sample every 60 seconds; page refresh is 15 seconds; stale threshold is 180 seconds; storage retains 240 samples per source and 200 fixed collection events. It excludes arbitrary logs, prompts and credentials. Client CPU sums a process family; server CPU measures the host. They have different scopes. Screenshot clocks are EDT, UTC−4. The 14:43:14 UTC Mac and 14:43:35 UTC EC2 screenshot samples precede the 14:50:25 UTC API readback.

The controlled desktop quit/relaunch produced a stopped-client sample at 14:44:42 UTC and a recovery sample at 14:49:34 UTC. These are sample times, not exact click times. The stopped-client screenshot shows zero desktop processes while server service checks remain healthy. Gateway PID 4991 and start time 14:26:10 UTC stayed unchanged per receipt. A Current badge means freshness, not health. This proves process independence; active model-session survival remains unverified. Collection events are operational telemetry, separate from security audit/SEL events.

## Cost, boundaries and browser artifact

Recorded price evidence: t4g.xlarge compute $0.1344/hour plus public IPv4 $0.005/hour, total $0.1394/hour. Active 40 GiB gp3 costs $3.20/month; stopped 24 GiB rollback disk $1.92/month. At 730 running hours, combined base cost is $106.882, approximately $107/month. Model use, S3, transfer and tax add charges. Standard CPU credits avoid surplus-credit charges but sustained load can throttle to baseline. No NAT gateway or load balancer. Stopping compute leaves disks billable; the address may change on restart.

One operator and one execution host are explicit demo assumptions. Native-session capacity, latency, multi-user isolation, failover, recovery targets and protected external security-event storage are not established. Central policy distribution, SSM transport, EDR and external SEL retention are proposals. Preserve the single accepted endpoint-control reference image; it includes capabilities beyond verified demo behavior. The current ARM diagram marks the native Gateway→MCP path pending.

The browser delivery receipt binds the current HTML, notes and all fourteen JPEGs. It records 14 desktop slides at 1280×720 with zero detected overflow/footer overlap, mobile layout checks at 390×844, navigation, notes, overview, image expansion, fullscreen and scroll-reset checks. Static validation passed. Browser download prompts and printing were not exercised. This council reviews visual/text artifacts, not interactive behavior. Prior Grok/Opus reviews covered the earlier frozen PPTX candidate; they do not cover these HTML bytes. A new no-ai-slop pass will follow incorporation of this council's decisions.

Local provenance inspected by dispatcher: `HANDOFF.md`; `evidence/presentation/kirocrew-system-design/build-manifest.json`; `delivery-receipt.json`; `static-validation.json`; `interactions.json`; and the complete current notes. Underlying runtime receipt filenames are cited in notes; their contents and live services are not included in this packet.
