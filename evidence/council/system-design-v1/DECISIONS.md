# Decisions for the system-design presentation

Grok 4.6 through Grok Build and Opus 5 through Claude Code reviewed frozen HTML `bfc2516128526ab049a7c4c0b36994aae82d87c44790785ee9125da5d63a5712`, all fourteen slide images and the complete notes. Both returned CHANGES REQUIRED. Both launches requested xhigh. The dispatcher verified the model identities and every exact image payload; provider-internal reasoning effort is not independently visible.

Root read both complete reviews and compared disputed claims against the existing artifacts and receipts. These decisions apply to the revised browser presentation:

| Slides | Decision | Basis and treatment |
| --- | --- | --- |
| 3 | Label the image as a September 11 reference, and name writable configuration and unverified policy/path enforcement. | Both reviewers found a risk of reading the broader reference as deployed capabilities. Preserve its single enclosure and exact image bytes. Keep September 13 ARM checks distinct. |
| 4 | Draw the verified direct probe as a separate, solid flow. | Both reviewers found the existing caption too easy to confuse with the dashed native path. Add a visible EC2 probe → MCP :8001 → S3 flow below the preserved image, without adding an endpoint enclosure. |
| 9 | Explain the displayed zero turn latency on the slide. | Both reviewers found the native-session caveat too far from the zero metrics. Add a visible empty-series explanation. Keep the screenshot intact; avoid claiming every zero-valued Gateway metric is invalid. |
| 3, 9–11 | Make image expansion discoverable. | Both reviewers found dense labels hard to read at slide scale. Show the Expand control continuously and add concise inspection cues. The original images remain available in the dialog. |
| 5 | Distinguish observed checks from role policy; identify operator and writable app code. | Accept Grok's observed-versus-configured distinction and writable App Kit finding, and Opus's SSH/sudo identity label. SSM policy attachment does not imply SSM execution was tested. Keep the actual IPv4 TCP denial scope. |
| 10 | Name the admin portal owner-token route. | Accept Grok's clarification. Portal authentication and CLI authentication use different paths. Preserve the loopback backend detail in notes. |
| 11 | State that collection events are operational telemetry. | Accept Opus's clarification. Crew-run collection events do not establish a protected SEL audit record. Preserve the UTC/EDT labels and receipt-bound PID/start time. |
| 12 | Name the stopped t3a.small and its retained 24 GiB disk. | Accept Grok's cost/provenance clarification. Keep the verified rate arithmetic and Standard credit behavior. |
| 14 | Mark native acceptance pending and state what to do if sign-in fails. | Accept Opus's live-demo finding. Stop the native sequence and report the failure. Preserve the requirement that crew_denied stops automatically before approval or MCP dispatch. |
| Header | Rename Original PPTX to Earlier PPTX (12 slides). | Opus inferred that the download was a superseded candidate. That inference is unsupported: decoded bytes match the final accepted ARM deck, SHA-256 37c82c82a00f16694f82fd11d2eda4c6188ce49de233b0c52448c47b820e0c13. Keep the download and clarify its edition. |
| 1 | Narrow telemetry verified to telemetry collection verified. | Actual telemetry collection and observations are receipted; Opus's suggestion that only enabled configuration exists is too narrow. The revised wording identifies the verified scope without implying native model metrics. |

The shared findings are resolved through visible labels, a separate direct-probe flow and persistent expansion controls. No screenshot was redrawn or annotated, no endpoint-control enclosure was added, and no live behavior was inferred from a review.

After incorporation, root applies No AI Slop to the full copy and notes, checks changed layouts in the browser, and binds the resulting artifacts in the delivery receipt. The council verdict remains attached to its frozen input. Later edits receive author validation, not an invented final-byte council approval.
