# Managed KiroCrew demo

The EC2 Gateway now loads a root-owned governance policy. The Crew owner can edit ordinary configuration, but the Crew service account cannot write that policy or its systemd configuration. A fresh request from the native Mac client was denied by the policy before it reached the MCP service. The earlier editable `crew_denied` hook was removed before this test.

This is the current guide, updated for the September 14 native host takes. The [status file](../evidence/native-client-demo/current-status.json) binds the delivered presentation and its validation. Earlier guides and clips retain their original dates and evidence scope.

## What is enforced

The Gateway reads `/etc/kirocrew-demo/security-policy.json` at startup with `fail_closed` behavior. It requires the `cc` sandbox level, denies YOLO mode, denies `@aws-enforcement/crew_denied`, pins the built-in S3 upload command rule, and adds the specified path and command restrictions. The policy file, its ancestors and the service drop-in are protected by root ownership. See the [policy plan](../evidence/enterprise-managed/20260913/policy-plan.json), [running-service verification](../evidence/enterprise-managed/20260913/policy-verify.json) and [MCP verification](../evidence/enterprise-managed/20260913/mcp-verify.json).

The dashboard terminal is disabled. A separate device policy provides a kernel backstop: a disposable process running as Crew inside the Gateway's actual service cgroup received `EPERM` when it tried to create a PTY. Existing Kiro CLI processes had `NoNewPrivs=1`, seccomp filters and separate mount namespaces. These are the bounded observations in the [host runtime check](../evidence/enterprise-managed/20260913/host-runtime-check.json); they do not establish every sandbox behavior during a particular tool call.

Host root remains the administrator. This deployment uses a local, unsigned policy file fetched at startup, an owner connection and the Linux `cc` sandbox. It has no enterprise SSO, managed Kiro CLI MCP registry or verified macOS Seatbelt enforcement. The configured YOLO denial does not make every other approval mode interactive.

## Native host results — September 14

Three additional native takes used the dedicated `host-controls-demo` agent on the same managed EC2 Gateway. They reached different boundaries:

| Request | Observed result | Attribution and limit |
|---|---|---|
| Read the public canary inside `/home/crew/.aws/` | Native `fs_read` returned CLI validation `ENOENT`; administrator snapshots confirmed that the exact 53-byte public fixture existed on the host. | The [native-only review](../evidence/managed-host-20260914/receipts/sensitive-read.json) retains `accepted:false`. A [separate later readback](../evidence/managed-host-20260914/receipts/sensitive-namespace.json) bound to the exact session's current CLI descendants found `.aws` covered by a tmpfs mount and the canary absent from their process view. That corroborates the mask without changing the native review's verdict, identifying the historical syscall PID or establishing a Crew hook denial. |
| Create the disposable marker inside `/home/crew/.kiro/agents/.demo-host-controls/` | The built-in `crew_write_protected_path_hook` denied the exact native creation request automatically, with no approval prompt. The target was absent before and after. | The [protected-write review](../evidence/managed-host-20260914/receipts/protected-write.json) identifies the protected-config hook before the managed filesystem policy. This take does not independently test that policy's path rules or kernel write isolation. |
| Execute the fixed IPv4 metadata TCP helper | The native result reported `connected=false`, `errno=113`, zero application bytes and no metadata request. The helper source read and its execution were each approved once, separately. | The [IMDS review](../evidence/managed-host-20260914/receipts/imds-tcp.json) binds the unchanged, unique first OUTPUT reject rule for Crew UID 999 and `169.254.169.254/32`, whose counter increased from two packets to three. This supports rejection of this TCP attempt; it does not test IPv6, HTTP, credential retrieval or all egress. |

The [findings](../evidence/managed-host-20260914/findings.md) and [receipt index](../evidence/managed-host-20260914/index.json) collect these boundaries. The [before/after snapshots](../evidence/managed-host-20260914/receipts/host-state-snapshots.json) bind the same stable Gateway process during the three takes, following the overnight restart. Use [the host scenarios](HOST-CONTROL-SCENARIOS.md) and [fresh-take review workflow](NATIVE-HOST-REVIEW.md) for the exact fixtures and evidence requirements. Native tool records, host readbacks and bounded counters establish these results; the model's explanation does not identify the enforcing layer. The earlier unexecuted or model-only attempts remain historical observations.

The TCP helper deliberately reports `native_enforcement_verified:false`: it reports its socket result, while the separate reviewer joins the native invocation to the firewall snapshots. That correlation covers one isolated execution and one rejected packet; no per-packet native tool-call identifier or executing-helper PID sample was captured.

## Walk through the controls

1. Open the native Mac client and confirm the remote Gateway connection. The [September 14 client readback](../evidence/managed-host-20260914/client-state.json) verified Nightly `0.7.0-nightly.20260913t061222`, local Gateway off, no listener on `5476` and the selected remote entry's loopback SSH tunnel on `5599`. The native tests used the original Kiro CLI backend on EC2. The [earlier quit/relaunch observation](../evidence/enterprise-managed/20260913/client-relaunch.json) retains its own date and process identities.
2. Open **Settings → Security → Governance**. Show the active file policy, its rule counts and the `cc` floor. Open the command rules, search for `s3`, and show the pinned upload rule with its disabled switch and organization-policy explanation.
3. Open **Connections → MCP Servers**. The `aws-enforcement` server is online with four tools and enabled for Crew only. The provider-global settings stay off. Stage a tool restriction to show **Apply** and **Discard**, then discard the preview. These toggles control tool availability; the UI does not display a separate policy-lock badge for each tool. Follow [MCP-GOVERNANCE.md](MCP-GOVERNANCE.md) for the user-facing manager and session inventory.
4. In a dedicated `enforcement-demo` conversation, request `@aws-enforcement/read_allowed` once with a fresh trace. Read the native approval card and approve that exact request once. In the completed `ui3` test, S3 returned the expected 55-byte fixture and matching digest. That successful read has [native and service evidence](../evidence/enterprise-managed/20260913/native-managed-verification.json), but no retained video.
5. Request `@aws-enforcement/crew_denied` once with another fresh trace. Stop after the denial. Expand the native blocked notice to show **Blocked by governance policy** and the exact tool pattern. The separate [ui4 take](../evidence/enterprise-managed/20260913/native-managed-ui4-verification.json) recorded this automatic denial without a permission prompt. Its completed service interval contained no MCP audit arrivals. The [40-second clip review](../evidence/enterprise-managed/20260913/media-review.json) covers the request, block and expanded reason; it does not include the earlier allowed read.
6. For the host demonstrations, create a separate persistent conversation with `host-controls-demo` selected at birth, complete a READY-only exchange and wait for it to become idle before attaching the observer. Run the sensitive read, disposable protected write and fixed IMDS TCP helper as separate takes using [NATIVE-HOST-REVIEW.md](NATIVE-HOST-REVIEW.md). The helper may request a source read first: approve that exact read once, then approve the exact execution separately. Stop each case after its result without an alternative route.
7. Use the admin tour to connect each result to its control and then inspect client/server telemetry. Telemetry identifies activity and collection state; use native tool records, policy events and the MCP audit to attribute security decisions.

For a new MCP take, use a new trace, one exact tool request and no retry or alternative route. Preserve the native tool-call ID and the complete server observation interval. The policy event has no direct native tool-call or trace ID, so its correlation uses the isolated session, exact tool, reason, timestamp and adjacent event linkage.

Check that the visible native app, accessibility text and captured image agree before recording. A normal full app quit/relaunch repaired stale capture state during the September 14 work without restarting the EC2 Gateway. Recheck the saved remote connection and visible conversation afterward. **View → Reload** left a black view and should not be used as that repair. Let timed macOS video capture end normally, then verify the MOV with ffprobe before trimming.

## Recreate it from this project

Invoke the project skill with `$kirocrew-demo`, or read [its setup, presentation and recording routes](../.agents/skills/kirocrew-demo/SKILL.md). The [dependency manager](DEPENDENCIES.md) plans and installs the selected workflow's dependencies. Playback needs Python 3.9 or newer and the checked-in artifacts. Authoring and live setup use an isolated Python 3.12 environment. Reviewer products are optional for playback.

The [live setup guide](LIVE-SETUP.md) covers ten explicit stages: runtime, services, fixtures, server integrations, managed policy, desktop, client telemetry, app, MCP user UI and host controls. Use the [ARM runtime bundler](ARM-RUNTIME-BUNDLE.md) to prepare the selected installed Nightly without copying user credentials or sessions. Each product uses its own sign-in, and a new deployment requires its own native acceptance tests.

Use [CloudFormation deployment](DEPLOYMENT.md) with an existing VPC and subnet. Put network values in a separate CloudFormation parameter file using the [public](../config/cloudformation-public.example.json) or [private](../config/cloudformation-private.example.json) example, and machine-specific connection values in the ignored `config/demo.local.json`. Keep SSH ingress to the client's single IPv4 `/32`. A private subnet needs an existing route from the Mac and the required outbound access; setup adds no VPC, subnet, VPN, bastion or NAT gateway. The selected server is ARM `t4g.xlarge` in `us-east-1`.

The three September 14 host takes provide the specific observations above. The sensitive read remains a path-unavailable native result with separate namespace corroboration, not an accepted automatic-hook denial. These takes do not establish every sandbox behavior or replace a new deployment's acceptance checks. The earlier command clip shows only the response-summary inspection, and the private-memory clip shows a conversation-assignment refusal rather than restricted human-role RBAC. See the [dated host scenarios](HOST-CONTROL-SCENARIOS.md) before repeating them. Keep GitHub Actions out of this project.
